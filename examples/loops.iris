
fn licm_nested(n: f64, a: f64, b: f64) -> f64 {
  var sum = 0
  var i = 0
  while (i < 100) {
    var x = a * b
    var j = 0
    while (j < n) {
      var y = x + 1
      sum = sum + y
      j = j + 1
    }
    i = i + 1
  }
  return sum
}
