fn gcd(a: f64, b: f64) -> f64 {
  if (b == 0) {
    return a
  } else {
    return gcd(b, a - (a / b) * b)
  }
}

fn gcd_iterative(a: f64, b: f64) -> f64 {
  var temp = 0
  while (b != 0) {
    temp = b
    b = a - (a / b) * b
    a = temp
  }
  return a
}
